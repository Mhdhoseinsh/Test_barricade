services:
  - type: web
    name: route9-game-server
    env: node
    plan: free
    # اختیاری. اگر این خط را ننویسی، Render همیشه سرور را در Oregon
    # (آمریکا) بالا می‌آورد — از نظر جغرافیایی تقریباً دورترین گزینه به
    # ایران، و همین فاصله‌ی فیزیکی خودش چند صد میلی‌ثانیه تأخیر واقعی به
    # هر رفت‌وبرگشتِ پیام اضافه می‌کند (جدا از هر VPN/پراکسی که کاربر
    # استفاده می‌کند). frankfurt نزدیک‌ترین ریجن Render به ایرانه و
    # می‌تونه این بخش از تأخیر رو محسوس کم کنه.
    # نکته‌ی مهم: Render اجازه نمی‌ده ریجنِ یک سرویسِ از قبل ساخته‌شده
    # عوض بشه — یعنی همین خط به‌تنهایی چیزی رو عوض نمی‌کنه. برای واقعاً
    # استفاده کردن ازش باید یه سرویس جدید با همین render.yaml بسازی (نه
    # ادیت سرویس فعلی)، بعد آدرس جدیدش رو جای GAME_SERVER_URL توی room.js
    # و توی متای CSP بالای index.html بذاری. تا وقتی این کار رو نکردی،
    # همه‌چیز دقیقاً مثل الان کار می‌کنه — این خط فقط برای وقتیه که
    # بخوای این قدم رو برداری.
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      # اختیاری. اگر پر شود، فقط همین آدرس(ها) اجازه‌ی اتصال Socket.io به
      # این سرور را دارند (چند مقدار را با کاما جدا کنید). اگر خالی
      # بماند، سرور دقیقاً مثل قبل از هر آدرسی اتصال را می‌پذیرد — یعنی
      # خالی‌گذاشتنش هیچ چیزی را خراب نمی‌کند. مقدار را در داشبورد Render
      # زیر Environment وارد کنید، مثلاً:
      #   https://your-username.github.io
      - key: ALLOWED_ORIGINS
        sync: false
